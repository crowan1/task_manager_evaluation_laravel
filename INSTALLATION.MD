Pour que l'application fonctionne

- Lancer les commandes : 

```composer install ```
```npm install ```

```php artisan key:generate```

```touch database/database.sqlite```
```php artisan migrate```

- Créer un fichier .env y mettre le contenu de .env.example (mettre le port sql configuré dans votre PC)


