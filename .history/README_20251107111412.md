# Projet Laravel - Guide d'Installation

Ce guide vous explique comment cloner ce projet Laravel et le reconstruire sur une nouvelle machine.

## 📋 Prérequis

Avant de commencer, assurez-vous d'avoir installé :
- PHP >= 8.2
- Composer
- SQLite ou MySQL/MariaDB
- Git

## 🚀 Installation sur une Nouvelle Machine

### Étape 1 : Cloner le Dépôt

```bash
git clone https://github.com/votre-nom/laravel_evaluation.git
cd laravel_evaluation
```

### Étape 2 : Installer les Dépendances PHP

```bash
composer install
```

Cette commande va recréer le dossier `vendor` avec toutes les bibliothèques PHP nécessaires.

### Étape 3 : Configurer l'Environnement

1. **Créer le fichier `.env`** à partir du modèle :

```bash
cp .env.example .env
```

2. **Générer la clé d'application** (essentiel pour la sécurité) :

```bash
php artisan key:generate
```

3. **Configurer la base de données** dans le fichier `.env` :

Pour SQLite (par défaut) :
```
DB_CONNECTION=sqlite
```

Pour MySQL :
```
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=nom_de_votre_base
DB_USERNAME=votre_utilisateur
DB_PASSWORD=votre_mot_de_passe
```

### Étape 4 : Finaliser l'Installation

1. **Créer le lien de stockage** (pour les fichiers uploadés) :

```bash
php artisan storage:link
```

2. **Exécuter les migrations** pour créer la structure de la base de données :

```bash
php artisan migrate
```

3. **(Optionnel) Peupler la base de données** avec des données de test :

```bash
php artisan db:seed
```

### Étape 5 : Démarrer le Serveur

```bash
php artisan serve
```

L'application sera accessible à l'adresse : `http://localhost:8000`

## 📦 Qu'est-ce qui est Versionné ?

Le fichier `.gitignore` est configuré pour **EXCLURE** :
- ❌ `/vendor/` - Les dépendances PHP (trop volumineuses)
- ❌ `.env` - Les configurations locales (sécurité)
- ❌ `/node_modules/` - Les dépendances JavaScript
- ❌ Les fichiers de cache et logs

Le dépôt Git contient **UNIQUEMENT** :
- ✅ Le code source de l'application
- ✅ `composer.json` et `composer.lock` (liste des dépendances)
- ✅ `.env.example` (modèle de configuration)
- ✅ Les fichiers de migration et seeders
- ✅ Les fichiers de configuration Laravel

## 🔧 Commandes Utiles

### Effacer le cache
```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Créer une nouvelle migration
```bash
php artisan make:migration nom_de_la_migration
```

### Créer un contrôleur
```bash
php artisan make:controller NomDuController
```

### Créer un modèle avec migration
```bash
php artisan make:model NomDuModele -m
```

## 🔄 Mettre à Jour le Projet

Après avoir récupéré les dernières modifications depuis GitHub :

```bash
git pull origin main
composer install
php artisan migrate
```

## 🆘 Résolution de Problèmes

### Erreur "No application encryption key has been specified"
```bash
php artisan key:generate
```

### Erreur de permissions sur le dossier storage
```bash
chmod -R 775 storage bootstrap/cache
```

### Réinitialiser complètement la base de données
```bash
php artisan migrate:fresh --seed
```

## 📝 Structure du Projet

```
laravel_evaluation/
├── app/                # Code de l'application (Modèles, Contrôleurs, etc.)
├── bootstrap/          # Fichiers de démarrage de Laravel
├── config/             # Fichiers de configuration
├── database/           # Migrations, factories, seeders
├── public/             # Point d'entrée web (index.php)
├── resources/          # Vues, assets non compilés
├── routes/             # Définition des routes (web.php, api.php)
├── storage/            # Fichiers générés, logs, cache
├── tests/              # Tests automatisés
├── vendor/             # Dépendances (NON versionné)
├── .env                # Configuration locale (NON versionné)
├── .env.example        # Modèle de configuration (versionné)
├── composer.json       # Dépendances PHP (versionné)
└── composer.lock       # Versions exactes des dépendances (versionné)
```

## 📧 Contact

Pour toute question, contactez : votre-email@example.com

---

**Bon développement ! 🎉**
